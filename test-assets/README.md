# Demo Assets

This directory contains intentionally insecure files used to demonstrate the security controls implemented in the CI/CD pipeline.

- `vulnerable.py` is used to validate the Semgrep SAST stage.
- `fake_private_key.pem` is a non-production demonstration key used to validate secret scanning with Gitleaks.

These files are included solely for educational and testing purposes.